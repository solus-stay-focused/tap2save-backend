PORT=8787

# Comma-separated list of frontend origins allowed to call this API.
# Use * only for local testing.
ALLOWED_ORIGINS=https://tap2save.net,http://localhost:3000

# Override if yt-dlp/ffmpeg aren't on PATH
YTDLP_PATH=yt-dlp
FFMPEG_PATH=ffmpeg
